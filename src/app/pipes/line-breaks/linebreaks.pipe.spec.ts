import { LinebreaksPipe } from './linebreaks.pipe';

describe('InebreaksPipe', () => {
  it('create an instance', () => {
    const pipe = new LinebreaksPipe();
    expect(pipe).toBeTruthy();
  });
});
